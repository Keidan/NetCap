#include "net.h"


int main(int argc, char** args) {
	// iface
	// filename
	// output
	//int fd = net_capture_open(nativeName);
	//nt r = net_capture_process(fds, len, &iCaptureCb, capture_cb);
  return 0;
}
